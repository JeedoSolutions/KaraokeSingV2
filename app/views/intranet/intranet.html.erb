<h1 class=""><i class="fa fa-cogs" aria-hidden="true"></i>
Mantenimientos</h1>
   <!--Menu2 Sidebar-->
    <div class="container">
      
      <div class="row">
        <div class="col-lg-9">
          <%= bootstrap_flash %>
            <div class="page-header">
              <h1><%= yield :header_title %> <small><%= yield :header_sub_title %></small>
            </h1>
           </div>
          <%= yield %>
        </div>
        <div class="col-lg-3">
          <div class="well sidebar-nav">
            <h3 class="">Mantenimientos</h3>
            <ul class="nav nav-list">
               <% if (@employee != nil) %>
                  
                   <% if (@employee['admin'] == true) %>
                        <%= render 'layouts/menu1' %>
                   <% end %>
               <% end %>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
      </div><!--/row-->
     </div>
